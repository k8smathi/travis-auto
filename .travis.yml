sudo: required
services:
  - docker
before_install:
  - docker build -t k8smathi/app:v1 .
script:
  - echo "$password" | docker login -u "$username" --password-stdin
  - docker images
  - docker push k8smathi/app:v1
notifications:
  email:
    recipients:
      - bvijaycom@gmail.com
    on_success: always
    on_failure: always
